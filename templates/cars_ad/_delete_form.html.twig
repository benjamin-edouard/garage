<form method="post" action="{{ path('app_cars_ad_delete', {'id': cars_ad.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cars_ad.id) }}">
    <button class="btn">Delete</button>
</form>
